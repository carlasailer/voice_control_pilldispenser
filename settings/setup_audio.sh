#!/bin/sh

#copy the file asound.conf to the correct location 
#ensures that the correct audio output devices are chosen

#sudo cp settings/asound.conf /etc/
sudo cp /var/lib/snips/skills/snips_app_pilldispenser/settings/asound.conf /etc/